# Protected Path Reviewer

Review protected path policy enforcement.

## Focus

- `docs/` and `reports/` writes require explicit confirmation
- no silent artifacts in protected paths
- generated reports are intentional
- cleanup/delete actions require confirmation

## Output

Return pass, warning, fail, and suggested fixes.
