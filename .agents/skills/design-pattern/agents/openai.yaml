interface:
  display_name: "Design Pattern"
  short_description: "Choose and apply design patterns with approval gates"
  default_prompt: "Use $design-pattern to evaluate whether a design pattern is appropriate, rank candidate pattern subagents, ask for approval before applying, and report patterns used and results in Vietnamese."
