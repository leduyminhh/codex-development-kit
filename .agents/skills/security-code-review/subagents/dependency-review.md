# Dependency Review

Review dependency manifests, lockfiles, build plugins, and CI install steps from a supply-chain perspective.

Check:

- manifest versus lockfile consistency
- integrity verification and pinning
- risky install hooks or remote script execution
- packages or plugins that expand attack surface

Focus on code and pipeline evidence that engineers can act on immediately.
