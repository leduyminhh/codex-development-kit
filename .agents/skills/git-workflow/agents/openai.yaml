interface:
  display_name: "Git Workflow"
  short_description: "Commit, branch, merge, revert, release, and push workflow"
  default_prompt: "Use $git-workflow to inspect changes, generate a conventional commit message and Vietnamese description when needed, avoid worktrees unless requested, and guide branch/commit/push flow safely."
